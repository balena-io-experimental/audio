name: Flowzone

on:
  pull_request:
    types: [opened, synchronize, closed]
    branches:
      - "main"
      - "master"

jobs:
  flowzone:
    name: Flowzone
    uses: product-os/flowzone/.github/workflows/flowzone.yml@master
    secrets: inherit
    with:
      balena_slugs: 'balenalabs/audio-rpi,balenalabs/audio-aarch64,balenalabs/audio-amd64,balenalabs/audio-armv7hf'
